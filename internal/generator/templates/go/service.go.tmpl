package {{.packageName}}

import (
     "github.com/gin-gonic/gin"
    {{- range $k,$v:=.importData}}
     {{$k|lowCamel}} "{{$v}}"
    {{- end}}
)
type {{.funcName}}Service struct {
    ctx *gin.Context
}
func New{{.funcName}}Service(ctx *gin.Context)*{{.funcName}}Service {
    return &{{.funcName}}Service{ctx:ctx}
}

func (s *{{.funcName}}Service){{.funcName}}(request  *{{.reqShortPath}}.{{.funcName}}Req) (  *{{.respShortPath}}.{{.funcName}}RespData,error) {
	resp := new({{.respShortPath|lowCamel}}.{{.funcName}}RespData)
	 //TODO
	return resp, nil

}
